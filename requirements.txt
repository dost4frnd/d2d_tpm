# Core scientific stack (versions are lower bounds; newer patch releases are fine).
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
PyYAML>=6.0
# PyTorch is required for the feed-forward / hybrid detector. Install the build
# that matches your platform (CPU wheel shown; use the CUDA wheel on Jetson/GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1

# Post-quantum: pure-Python FIPS 203 ML-KEM reference (no C toolchain needed).
# Timings are relative only; byte sizes match the standard. For optimized
# on-device numbers use liboqs / pqm4 instead.
kyber-py>=1.0

# --- Optional: deployment / acceleration (not needed for the simulation) ---
# onnx>=1.15            # PyTorch -> ONNX export (deployment/tensorrt)
# onnxruntime>=1.17     # portable accelerated inference fallback
# tensorrt              # Jetson only (ships with JetPack); for the .plan engine
# pycuda                # Jetson only; TensorRT runtime bindings
